package main.java.com.example.csvPlusPlus.DataModels;

//Unused outside of scope
public class StringColumnSynopsis {
    //number min, max, median, average, mode, std, nice tohave: normal dist
    //string top 5 common values and count
    // nice to have subtype date : min, max, normal dist

    public enum ColumnType {
        NUMBER, STRING;
    }

    private ColumnType columnType;



}
